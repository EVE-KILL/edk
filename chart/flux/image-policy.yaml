apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: edk-latest
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: edk
  policy:
    alphabetical:
      order: asc
  filterTags:
    pattern: '^latest$'
  digestReflectionPolicy: IfNotPresent
