# ClickHouse Configuration
# Host where ClickHouse is running (use 'clickhouse' for Docker Compose service name)
CLICKHOUSE_HOST=localhost
# HTTP interface port (default: 8123)
CLICKHOUSE_PORT=8123
CLICKHOUSE_HTTP_PORT=8123
# Native client port (default: 9000)
CLICKHOUSE_NATIVE_PORT=9000
# Database name to use
CLICKHOUSE_DB=edk
# Username for ClickHouse authentication
CLICKHOUSE_USER=edk_user
# Password for ClickHouse authentication
CLICKHOUSE_PASSWORD=your_clickhouse_password_here
# Full HTTP URL for ClickHouse
CLICKHOUSE_URL=http://localhost:8123

# Redis Configuration
# Host where Redis is running (use 'redis' for Docker Compose service name)
REDIS_HOST=localhost
# Redis port (default: 6379)
REDIS_PORT=6379
# Password for Redis authentication
REDIS_PASSWORD=your_redis_password_here
# Full Redis URL with authentication
REDIS_URL=redis://:your_redis_password_here@localhost:6379

# Database URLs (for application use)
# Complete connection string for ClickHouse
DATABASE_URL=clickhouse://edk_user:your_clickhouse_password_here@localhost:8123/edk
# Complete connection string for Redis (database 0)
REDIS_CONNECTION_STRING=redis://:your_redis_password_here@localhost:6379/0

# Additional Notes:
# - Change the passwords to secure values in production
# - When running with Docker Compose, you can use service names (clickhouse, redis) instead of localhost
# - For production, consider using Docker secrets or external secret management
# - The default Redis database is 0, you can change it by modifying the number at the end of the URL
