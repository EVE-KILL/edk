<!DOCTYPE html>
<html>
  <head>
    <meta charset='UTF-8' />
    <title>{{page.title}} - {{config.title}}</title>
  </head>
  <body>
    <header>
      <h1>{{config.title}}</h1>
    </header>
    <main>
      {{{body}}}
    </main>
    <footer>
      <p>{{config.copyright}} {{default.siteName}}</p>
    </footer>
  </body>
</html>