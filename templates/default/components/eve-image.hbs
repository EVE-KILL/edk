<img
  src="{{> partials/eve-image-url type=type id=id variant=variant size=size}}"
  alt="{{fallback alt name}}"
  class="{{fallback class ''}}"
  width="{{fallback width (roundImageSize (fallback size 64) type)}}"
  height="{{fallback height (roundImageSize (fallback size 64) type)}}"
  loading="{{fallback loading 'lazy'}}"
/>
