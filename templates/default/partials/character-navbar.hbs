<!-- Character Navigation Bar -->
<nav class="entity-nav">
    <div class="entity-nav__container">
        <div class="entity-nav__tabs">
            <a href="/character/{{character.id}}"
               class="entity-nav__tab {{#if (eq currentTab 'dashboard')}}entity-nav__tab--active{{/if}}">
                Dashboard
            </a>
            <a href="/character/{{character.id}}/kills"
               class="entity-nav__tab {{#if (eq currentTab 'kills')}}entity-nav__tab--active{{/if}}">
                Kills
            </a>
            <a href="/character/{{character.id}}/losses"
               class="entity-nav__tab {{#if (eq currentTab 'losses')}}entity-nav__tab--active{{/if}}">
                Losses
            </a>
        </div>
    </div>
</nav>

<style>
.entity-nav {
    background: var(--color-bg-tertiary);
    border: 1px solid var(--color-border-dark);
    border-radius: 4px;
    margin-bottom: 20px;
    overflow: hidden;
}

.entity-nav__container {
    display: flex;
    align-items: center;
    padding: 0;
}

.entity-nav__tabs {
    display: flex;
    gap: 0;
    width: 100%;
}

.entity-nav__tab {
    flex: 1;
    padding: 12px 16px;
    background: rgba(0, 0, 0, 0.2);
    border-right: 1px solid var(--color-border-dark);
    color: var(--color-text-secondary);
    text-decoration: none;
    font-size: 11px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    transition: all 0.2s ease;
    text-align: center;
    cursor: pointer;
}

.entity-nav__tab:last-child {
    border-right: none;
}

.entity-nav__tab:hover {
    background: rgba(74, 158, 255, 0.15);
    color: #4a9eff;
}

.entity-nav__tab--active {
    background: rgba(74, 158, 255, 0.2);
    color: #4a9eff;
    border-bottom: 2px solid #4a9eff;
}
</style>
