<!-- Entity Navigation Tabs -->
{{> components/tabs
    tabs=(array
        (obj id="dashboard" label="DASHBOARD" url=baseUrl)
        (obj id="kills" label="KILLS" url=(concat baseUrl "/kills"))
        (obj id="losses" label="LOSSES" url=(concat baseUrl "/losses"))
    )
    active=currentTab
}}
