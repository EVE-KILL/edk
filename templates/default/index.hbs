<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{page.title}} - {{default.siteName}}</title>
    <meta name="description" content="{{page.description}}">
    {{#if page.keywords}}
    <meta name="keywords" content="{{page.keywords}}">
    {{/if}}
    <meta property="og:type" content="{{#if page.type}}{{page.type}}{{else}}website{{/if}}">
    <meta property="og:title" content="{{page.title}} - {{default.siteName}}">
    <meta property="og:description" content="{{page.description}}">
    {{#if page.image}}
    <meta property="og:image" content="{{page.image}}">
    {{/if}}
    {{#if page.url}}
    <meta property="og:url" content="{{page.url}}">
    {{/if}}
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            line-height: 1.6;
            color: #e0e0e0;
            background: linear-gradient(135deg, #0a0a0a 0%, #1a1a2e 100%);
            min-height: 100vh;
        }
        header {
            background: rgba(0, 0, 0, 0.8);
            padding: 1rem 2rem;
            border-bottom: 2px solid #00d9ff;
            box-shadow: 0 2px 10px rgba(0, 217, 255, 0.3);
        }
        header h1 {
            color: #00d9ff;
            font-size: 2rem;
            text-transform: uppercase;
            letter-spacing: 2px;
        }
        nav {
            margin-top: 0.5rem;
        }
        nav a {
            color: #00d9ff;
            text-decoration: none;
            margin-right: 1.5rem;
            transition: color 0.3s;
        }
        nav a:hover {
            color: #00ffaa;
        }
        main {
            max-width: 1200px;
            margin: 2rem auto;
            padding: 2rem;
        }
        h2 {
            color: #00d9ff;
            margin-bottom: 1rem;
            font-size: 2.5rem;
        }
        p {
            margin-bottom: 1rem;
            font-size: 1.1rem;
            color: #b0b0b0;
        }
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1.5rem;
            margin: 2rem 0;
        }
        .stat-card {
            background: rgba(0, 0, 0, 0.6);
            padding: 1.5rem;
            border-radius: 8px;
            border: 1px solid #00d9ff;
            box-shadow: 0 4px 15px rgba(0, 217, 255, 0.2);
            transition: transform 0.3s, box-shadow 0.3s;
        }
        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 6px 20px rgba(0, 217, 255, 0.4);
        }
        .stat-label {
            color: #888;
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        .stat-value {
            color: #00d9ff;
            font-size: 2rem;
            font-weight: bold;
            margin-top: 0.5rem;
        }
        .section {
            background: rgba(0, 0, 0, 0.6);
            padding: 2rem;
            border-radius: 8px;
            margin: 2rem 0;
            border-left: 4px solid #00d9ff;
        }
        .section h3 {
            color: #00ffaa;
            margin-bottom: 1rem;
        }
        pre {
            background: #0a0a0a;
            padding: 1rem;
            border-radius: 4px;
            overflow-x: auto;
            border: 1px solid #333;
            color: #00ffaa;
        }
        footer {
            background: rgba(0, 0, 0, 0.8);
            padding: 1.5rem 2rem;
            text-align: center;
            margin-top: 4rem;
            border-top: 2px solid #00d9ff;
            color: #666;
        }
        .message-box {
            background: linear-gradient(135deg, rgba(0, 217, 255, 0.1) 0%, rgba(0, 255, 170, 0.1) 100%);
            padding: 1.5rem;
            border-radius: 8px;
            border: 2px solid #00d9ff;
            margin: 2rem 0;
        }
        .message-box p {
            color: #e0e0e0;
            font-size: 1.2rem;
            margin: 0;
        }
    </style>
</head>
<body>
    <header>
        <h1>{{default.siteName}}</h1>
        <nav>
            <a href="/">Home</a>
            <a href="/api/killmail/1/esi">API Docs</a>
            <a href="/health">Health</a>
        </nav>
    </header>

    <main>
        <h2>Welcome to EVE-KILL</h2>
        <p>Real-time killmail tracking and analytics for EVE Online</p>

        {{#if data.message}}
        <div class="message-box">
            <p>✓ {{data.message}}</p>
        </div>
        {{/if}}

        {{#if data.stats}}
        <div class="section">
            <h3>System Statistics</h3>
            <div class="stats-grid">
                <div class="stat-card">
                    <div class="stat-label">Total Killmails</div>
                    <div class="stat-value">{{formatNumber data.stats.killmails}}</div>
                </div>
                <div class="stat-card">
                    <div class="stat-label">Characters Tracked</div>
                    <div class="stat-value">{{formatNumber data.stats.characters}}</div>
                </div>
                <div class="stat-card">
                    <div class="stat-label">Corporations</div>
                    <div class="stat-value">{{formatNumber data.stats.corporations}}</div>
                </div>
                <div class="stat-card">
                    <div class="stat-label">Alliances</div>
                    <div class="stat-value">{{formatNumber data.stats.alliances}}</div>
                </div>
            </div>
        </div>
        {{/if}}

        {{#if data}}
        <div class="section">
            <h3>Debug Data</h3>
            <pre>{{json data}}</pre>
        </div>
        {{/if}}

        <div class="section">
            <h3>Features</h3>
            <ul style="color: #b0b0b0; margin-left: 2rem;">
                <li>Real-time killmail ingestion from EVE-KILL WebSocket feed</li>
                <li>Historical killmail data storage and querying</li>
                <li>Entity tracking (characters, corporations, alliances)</li>
                <li>EVE Online Static Data Export (SDE) integration</li>
                <li>High-performance analytics with ClickHouse</li>
            </ul>
        </div>
    </main>

    <footer>
        <p>&copy; {{default.year}} {{default.siteName}}. EVE Online and the EVE logo are the registered trademarks of CCP hf.</p>
        <p style="margin-top: 0.5rem; font-size: 0.9rem;">Environment: {{default.env}}</p>
    </footer>
</body>
</html>
