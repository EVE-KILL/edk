<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Primary Meta Tags -->
    <title>{{#if page.title}}{{page.title}}{{else if title}}{{title}}{{/if}}{{#unless page.skipSiteName}} - {{#if config.title}}{{config.title}}{{else}}EVE Kill{{/if}}{{/unless}}</title>
    <meta name="title" content="{{#if page.title}}{{page.title}}{{else if title}}{{title}}{{else}}Home{{/if}}{{#unless page.skipSiteName}} - {{#if config.title}}{{config.title}}{{else}}EVE Kill{{/if}}{{/unless}}" />
    <meta name="description" content="{{#if page.description}}{{page.description}}{{else if description}}{{description}}{{else}}Real-time EVE Online killmail tracking and analytics. View detailed killmail information, ship fittings, and combat statistics.{{/if}}" />
    <meta name="keywords" content="{{#if page.keywords}}{{page.keywords}}{{else if keywords}}{{keywords}}{{else}}eve online, killmail, killboard, pvp, zkillboard, edk, eve kill, ship losses, combat tracker{{/if}}" />
    <meta name="author" content="{{#if config.title}}{{config.title}}{{else}}EVE Kill{{/if}}" />
    <meta name="robots" content="index, follow" />
    <link rel="canonical" href="{{#if page.url}}{{config.siteUrl}}{{page.url}}{{else}}{{config.siteUrl}}{{/if}}" />

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="{{#if page.type}}{{page.type}}{{else}}website{{/if}}" />
    <meta property="og:url" content="{{#if page.url}}{{config.siteUrl}}{{page.url}}{{else}}{{config.siteUrl}}{{/if}}" />
    <meta property="og:title" content="{{#if page.title}}{{page.title}}{{else if title}}{{title}}{{else}}Home{{/if}}{{#unless page.skipSiteName}} - {{#if config.title}}{{config.title}}{{else}}EVE Kill{{/if}}{{/unless}}" />
    <meta property="og:description" content="{{#if page.description}}{{page.description}}{{else if description}}{{description}}{{else}}Real-time EVE Online killmail tracking and analytics. View detailed killmail information, ship fittings, and combat statistics.{{/if}}" />
    <meta property="og:image" content="{{#if page.image}}{{page.image}}{{else}}{{config.siteUrl}}/og-default.png{{/if}}" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:site_name" content="{{#if config.title}}{{config.title}}{{else}}EVE Kill{{/if}}" />
    <meta property="og:locale" content="en_US" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:url" content="{{#if page.url}}{{config.siteUrl}}{{page.url}}{{else}}{{config.siteUrl}}{{/if}}" />
    <meta name="twitter:title" content="{{#if page.title}}{{page.title}}{{else if title}}{{title}}{{else}}Home{{/if}}{{#unless page.skipSiteName}} - {{#if config.title}}{{config.title}}{{else}}EVE Kill{{/if}}{{/unless}}" />
    <meta name="twitter:description" content="{{#if page.description}}{{page.description}}{{else if description}}{{description}}{{else}}Real-time EVE Online killmail tracking and analytics. View detailed killmail information, ship fittings, and combat statistics.{{/if}}" />
    <meta name="twitter:image" content="{{#if page.image}}{{page.image}}{{else}}{{config.siteUrl}}/og-default.png{{/if}}" />
    {{#if config.twitterHandle}}
    <meta name="twitter:site" content="{{config.twitterHandle}}" />
    <meta name="twitter:creator" content="{{config.twitterHandle}}" />
    {{/if}}

    <!-- Additional Meta Tags -->
    <meta name="theme-color" content="#1a1a1a" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />

    {{#if page.structuredData}}
    <!-- JSON-LD Structured Data -->
    <script type="application/ld+json">
    {{{page.structuredData}}}
    </script>
    {{/if}}

    <link rel="stylesheet" type="text/css" href="/edk.css" />

    {{#if additionalCSS}}
    {{#each additionalCSS}}
    <link rel="stylesheet" type="text/css" href="{{this}}" />
    {{/each}}
    {{/if}}

    <!-- Dropdown Menu Styles -->
    <style>
        /* Soft Aesthetic Navigation Bar */
        .navigation {
            background: rgba(255, 255, 255, 0.03);
            border-top: 1px solid var(--color-border-dark);
            border-bottom: 1px solid var(--color-border-dark);
            padding: 0;
            margin: 0;
            width: 100%;
        }

        .navigation table {
            width: 100%;
            table-layout: fixed;
            background: transparent;
            border: none;
            border-radius: var(--radius-sm);
            padding: 0;
            margin: 0;
            gap: 0;
        }

        .navigation td {
            text-align: center;
            position: relative;
            padding: 0;
            flex: 1;
            background: transparent;
            border: none;
        }

        .navigation td:not(:last-child) {
            border-right: 1px solid rgba(255, 255, 255, 0.05);
        }

        .navigation td.nav-help {
            width: 40px;
            flex: 0 0 40px;
        }

        .navigation td.nav-user {
            width: 40px;
            flex: 0 0 40px;
        }

        .navigation td > a {
            display: block;
            width: 100%;
            box-sizing: border-box;
            padding: 10px 12px;
            color: var(--color-text-secondary);
            text-decoration: none;
            font-size: 11px;
            font-weight: 600;
            letter-spacing: 0.5px;
            text-transform: uppercase;
            transition: color 0.2s ease, background-color 0.2s ease;
        }

        .navigation td > a:hover {
            color: #4a9eff;
            background-color: rgba(74, 158, 255, 0.08);
        }

        /* Dropdown Styles */
        .nav-dropdown {
            overflow: visible;
        }

        .nav-dropdown-wrapper {
            position: relative;
            display: block;
            width: 100%;
        }

        .nav-dropdown-wrapper > a {
            cursor: pointer;
            display: block;
            width: 100%;
            text-align: center;
            box-sizing: border-box;
        }

        .nav-dropdown-content {
            display: none;
            position: absolute;
            background: rgba(26, 26, 26, 0.95);
            backdrop-filter: blur(10px);
            min-width: 200px;
            width: 100%;
            z-index: 1000;
            border: 1px solid var(--color-border-dark);
            border-radius: var(--radius-sm);
            margin-top: 4px;
            left: 0;
            top: 100%;
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.4);
        }

        /* Right-aligned dropdown for items on right side of screen */
        .nav-dropdown-content.align-right {
            left: auto;
            right: 0;
        }

        .nav-user {
            position: relative;
            height: 100%;
        }

        .nav-user .avatar-trigger {
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 4px 6px;
            cursor: pointer;
        }

        .nav-user .avatar-trigger img {
            width: 26px;
            height: 26px;
            border-radius: 50%;
            border: 1px solid var(--color-border-dark);
            box-shadow: 0 0 0 2px rgba(74, 158, 255, 0.1);
        }

        .nav-user .user-dropdown {
            display: none;
            position: absolute;
            right: 0;
            top: 100%;
            min-width: 240px;
            background: rgba(20, 20, 20, 0.96);
            border: 1px solid var(--color-border-dark);
            border-radius: 6px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.45);
            padding: 12px;
            z-index: 1000;
        }

        .nav-user:hover .user-dropdown,
        .nav-user:focus-within .user-dropdown {
            display: block;
        }

        .nav-user .user-header {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 10px;
        }

        .nav-user .user-header img {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            border: 1px solid var(--color-border-dark);
        }

        .nav-user .user-meta {
            display: flex;
            flex-direction: column;
            gap: 3px;
        }

        .nav-user .user-meta .name {
            font-size: 12px;
            font-weight: 700;
            color: #e7ecf5;
            letter-spacing: 0.3px;
        }

        .nav-user .user-meta .line {
            font-size: 11px;
            color: var(--color-text-tertiary);
        }

        .nav-user .user-actions {
            display: flex;
            gap: 8px;
            margin-top: 8px;
        }

        .nav-user .user-actions a {
            flex: 1;
            text-align: center;
            font-size: 11px;
            color: var(--color-text-secondary);
            text-decoration: none;
            padding: 8px 10px;
            border: 1px solid var(--color-border-dark);
            border-radius: 4px;
            transition: color 0.2s ease, background-color 0.2s ease, border-color 0.2s ease;
        }

        .nav-user .user-actions a:hover {
            color: #4a9eff;
            border-color: rgba(74, 158, 255, 0.7);
            background-color: rgba(74, 158, 255, 0.08);
        }

        .nav-user .login-link {
            display: inline-block;
            padding: 8px 12px;
            font-size: 11px;
            font-weight: 700;
            letter-spacing: 0.5px;
            text-transform: uppercase;
            color: #4a9eff;
            text-decoration: none;
            border: 1px solid rgba(74, 158, 255, 0.5);
            border-radius: 4px;
            background: rgba(74, 158, 255, 0.05);
            transition: all 0.2s ease;
        }

        .nav-user .login-link:hover {
            background: rgba(74, 158, 255, 0.15);
            border-color: rgba(74, 158, 255, 0.8);
            color: #7ab6ff;
        }

        /* Multi-column layout for dropdowns with many items */
        .nav-dropdown-content.multi-column {
            display: none;
            column-count: 3;
            column-gap: 0;
            width: auto;
            min-width: 600px;
            max-width: 800px;
            padding: 8px 0;
        }

        .nav-dropdown-content.multi-column > * {
            break-inside: avoid;
            page-break-inside: avoid;
        }

        .nav-dropdown-content a {
            color: var(--color-text-secondary);
            padding: 10px 15px;
            text-decoration: none;
            display: block;
            text-align: left;
            font-size: 11px;
            transition: color 0.2s ease, background-color 0.2s ease;
            border: none;
        }

        .nav-dropdown-content a:hover {
            background-color: rgba(74, 158, 255, 0.1);
            color: #4a9eff;
        }

        .nav-dropdown:hover .nav-dropdown-content {
            display: block;
        }

        .nav-dropdown:hover .nav-dropdown-content.multi-column {
            display: block;
            column-count: 3;
        }

        .nav-dropdown-wrapper:hover .nav-dropdown-content {
            display: block;
        }

        .nav-dropdown-wrapper:hover .nav-dropdown-content.multi-column {
            display: block;
            column-count: 3;
        }

        .dropdown-section-header {
            color: var(--color-text-tertiary);
            padding: 8px 15px;
            font-weight: 700;
            font-size: 10px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            background: transparent;
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
            margin: 8px 0 0 0;
        }

        .dropdown-section-header:first-child {
            margin: 0;
        }

        /* Search Box Styles - Soft Aesthetic */
        .nav-search {
            position: relative;
            overflow: visible !important;
            padding: 10px 12px !important;
        }

        .search-container {
            position: relative;
            width: 100%;
            padding: 0;
            margin: 0;
        }

        #globalSearch {
            width: 100%;
            padding: 8px 12px;
            font-size: 11px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            background: rgba(255, 255, 255, 0.02);
            color: var(--color-text-secondary);
            border-radius: var(--radius-sm);
            box-sizing: border-box;
            font-weight: 600;
            letter-spacing: 0.3px;
            transition: all 0.2s ease;
        }

        #globalSearch:focus {
            outline: none;
            border-color: #4a9eff;
            background: rgba(74, 158, 255, 0.05);
            color: var(--color-text-primary);
        }

        #globalSearch::placeholder {
            color: var(--color-text-tertiary);
            font-size: 11px;
        }

        .search-results {
            display: none;
            position: absolute;
            top: calc(100% + 4px);
            left: 0;
            right: 0;
            min-width: 300px;
            background: rgba(26, 26, 26, 0.98);
            backdrop-filter: blur(10px);
            border: 1px solid var(--color-border-dark);
            border-radius: var(--radius-sm);
            max-height: 500px;
            overflow-y: auto;
            z-index: 1000;
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.4);
        }

        .search-results.active {
            display: block;
        }

        .search-group {
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
        }

        .search-group:last-child {
            border-bottom: none;
        }

        .search-group-header {
            color: var(--color-text-tertiary);
            padding: 8px 15px;
            font-weight: 700;
            font-size: 10px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            background: transparent;
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
        }

        .search-group-label {
            color: var(--color-text-tertiary);
            padding: 8px 15px;
            font-weight: 700;
            font-size: 10px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            background: rgba(0, 0, 0, 0.2);
        }

        .search-result-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 15px;
            cursor: pointer;
            transition: all 0.15s ease;
            border-bottom: 1px solid rgba(255, 255, 255, 0.02);
        }

        .search-result-item:hover,
        .search-result-item.selected {
            background-color: rgba(74, 158, 255, 0.1);
        }

        .search-result-name {
            font-weight: 600;
            font-size: 11px;
            color: var(--color-text-secondary);
        }

        .search-result-item:hover .search-result-name,
        .search-result-item.selected .search-result-name {
            color: var(--color-accent-blue);
        }

        .search-result-type {
            font-size: 9px;
            color: var(--color-text-tertiary);
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .search-item {
            display: block;
            padding: 10px 15px;
            color: var(--color-text-secondary);
            text-decoration: none;
            transition: all 0.2s ease;
            border: none;
            font-size: 11px;
        }

        .search-item:hover,
        .search-item.active {
            background-color: rgba(74, 158, 255, 0.1);
            color: #4a9eff;
        }

        .search-item-name {
            display: block;
            font-weight: 700;
            margin-bottom: 2px;
            font-size: 11px;
        }

        .search-item-description {
            display: block;
            font-size: 10px;
            color: var(--color-text-tertiary);
        }

        .search-no-results,
        .search-error {
            padding: 20px 15px;
            text-align: center;
            color: var(--color-text-tertiary);
            font-size: 11px;
        }

        .search-error {
            color: #ef4444;
        }

        /* Spotlight Search Modal */
        .spotlight-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.8);
            backdrop-filter: blur(4px);
            z-index: 10000;
            animation: spotlightFadeIn 0.15s ease;
        }

        .spotlight-overlay.active {
            display: flex;
            align-items: flex-start;
            justify-content: center;
            padding-top: 15vh;
        }

        @keyframes spotlightFadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        .spotlight-modal {
            width: 90%;
            max-width: 640px;
            background: var(--color-bg-secondary);
            border: 1px solid var(--color-border-dark);
            border-radius: var(--radius-md);
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
            animation: spotlightSlideIn 0.2s ease;
            max-height: 70vh;
            display: flex;
            flex-direction: column;
        }

        @keyframes spotlightSlideIn {
            from {
                transform: translateY(-20px);
                opacity: 0;
            }
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

        .spotlight-header {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 16px 20px;
            border-bottom: 1px solid var(--color-border-dark);
        }

        .spotlight-icon {
            color: var(--color-text-tertiary);
            flex-shrink: 0;
        }

        #spotlightInput {
            flex: 1;
            background: transparent;
            border: none;
            outline: none;
            color: var(--color-text-secondary);
            font-size: 16px;
            font-weight: 500;
        }

        #spotlightInput::placeholder {
            color: var(--color-text-tertiary);
        }

        .spotlight-kbd {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            min-width: 36px;
            height: 24px;
            padding: 0 8px;
            background: var(--color-bg-tertiary);
            border: 1px solid var(--color-border-dark);
            border-radius: var(--radius-sm);
            color: var(--color-text-tertiary);
            font-size: 11px;
            font-weight: 600;
            font-family: monospace;
        }

        .spotlight-results {
            overflow-y: auto;
            max-height: calc(70vh - 72px);
        }

        .spotlight-section {
            border-bottom: 1px solid var(--color-border-dark);
        }

        .spotlight-section:last-child {
            border-bottom: none;
        }

        .spotlight-section-title {
            padding: 12px 20px 8px;
            font-size: 11px;
            font-weight: 700;
            color: var(--color-text-tertiary);
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .spotlight-result-item {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 12px 20px;
            cursor: pointer;
            transition: background 0.1s ease;
            border-bottom: 1px solid rgba(255, 255, 255, 0.02);
        }

        .spotlight-result-item:hover,
        .spotlight-result-item.selected {
            background: rgba(74, 158, 255, 0.1);
        }

        .spotlight-result-content {
            flex: 1;
        }

        .spotlight-result-name {
            font-size: 13px;
            font-weight: 600;
            color: var(--color-text-secondary);
            margin-bottom: 2px;
        }

        .spotlight-result-item:hover .spotlight-result-name,
        .spotlight-result-item.selected .spotlight-result-name {
            color: var(--color-accent-blue);
        }

        .spotlight-result-type {
            font-size: 10px;
            color: var(--color-text-tertiary);
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .spotlight-result-arrow {
            color: var(--color-text-tertiary);
            font-size: 18px;
            opacity: 0;
            transition: opacity 0.1s ease;
        }

        .spotlight-result-item:hover .spotlight-result-arrow,
        .spotlight-result-item.selected .spotlight-result-arrow {
            opacity: 1;
        }

        .spotlight-quick-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
            gap: 8px;
            padding: 0 20px 16px;
        }

        .spotlight-quick-item {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 10px 12px;
            background: var(--color-bg-tertiary);
            border: 1px solid var(--color-border-dark);
            border-radius: var(--radius-sm);
            color: var(--color-text-secondary);
            text-decoration: none;
            font-size: 12px;
            font-weight: 500;
            transition: all 0.15s ease;
        }

        .spotlight-quick-item:hover {
            background: rgba(74, 158, 255, 0.1);
            border-color: var(--color-accent-blue);
            color: var(--color-accent-blue);
        }

        .spotlight-recent-list {
            padding: 0 20px 16px;
            display: flex;
            flex-direction: column;
            gap: 6px;
        }

        .spotlight-recent-item {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 8px 12px;
            background: transparent;
            border: 1px solid var(--color-border-dark);
            border-radius: var(--radius-sm);
            color: var(--color-text-tertiary);
            font-size: 12px;
            cursor: pointer;
            transition: all 0.15s ease;
            width: 100%;
            text-align: left;
        }

        .spotlight-recent-item:hover {
            background: rgba(74, 158, 255, 0.05);
            border-color: var(--color-accent-blue);
            color: var(--color-text-secondary);
        }

        .spotlight-empty {
            padding: 20px 0;
        }

        .spotlight-loading,
        .spotlight-no-results,
        .spotlight-error {
            padding: 60px 20px;
            text-align: center;
            color: var(--color-text-tertiary);
            font-size: 13px;
        }

        .spotlight-no-results-icon {
            font-size: 48px;
            margin-bottom: 12px;
            opacity: 0.5;
        }

        .spotlight-no-results-text {
            color: var(--color-text-tertiary);
        }

        @media (max-width: 768px) {
            .spotlight-overlay.active {
                padding-top: 10vh;
            }

            .spotlight-modal {
                width: 95%;
                max-height: 80vh;
            }

            .spotlight-quick-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>

    {{{headerContent}}}
</head>
<body>
    <!-- Popup container for modals -->
    <div id="popup"></div>

    <!-- Main container using modern flexbox but EDK styling -->
    <div class="main-wrapper">
        <!-- Header / Banner -->
        <header>
            <h1>
                {{#if config.title}}{{config.title}}{{else}}EDK{{/if}}
            </h1>
            {{#if config.subtitle}}
            <p>{{config.subtitle}}</p>
            {{/if}}
        </header>

        <!-- Navigation Menu -->
        <nav class="navigation">
            <table cellspacing="0" cellpadding="0">
                <tr>
                    <td><a href="/">Home</a></td>
                    <td class="nav-dropdown">
                        <div class="nav-dropdown-wrapper">
                            <a href="/kills/latest">Kills</a>
                            <div class="nav-dropdown-content multi-column">
                                <div class="dropdown-section-header">Main</div>
                                <a href="/kills/latest">Latest</a>
                                <a href="/kills/big">Big Kills</a>
                                <a href="/kills/solo">Solo</a>
                                <a href="/kills/npc">NPC</a>
                                <a href="/kills/awox">AWOX</a>
                                <div class="dropdown-section-header">Security Status</div>
                                <a href="/kills/highsec">High-Sec</a>
                                <a href="/kills/lowsec">Low-Sec</a>
                                <a href="/kills/nullsec">Null-Sec</a>
                                <a href="/kills/w-space">W-Space</a>
                                <a href="/kills/abyssal">Abyssal</a>
                                <a href="/kills/pochven">Pochven</a>
                                <div class="dropdown-section-header">Ship Classes</div>
                                <a href="/kills/frigates">Frigates</a>
                                <a href="/kills/destroyers">Destroyers</a>
                                <a href="/kills/cruisers">Cruisers</a>
                                <a href="/kills/battlecruisers">Battlecruisers</a>
                                <a href="/kills/battleships">Battleships</a>
                                <a href="/kills/capitals">Capitals</a>
                                <a href="/kills/supercarriers">Supercarriers</a>
                                <a href="/kills/titans">Titans</a>
                                <a href="/kills/freighters">Freighters</a>
                                <a href="/kills/industrials">Industrials</a>
                                <div class="dropdown-section-header">Structures</div>
                                <a href="/kills/citadels">Citadels</a>
                                <a href="/kills/structures">All Structures</a>
                                <div class="dropdown-section-header">Tech Level</div>
                                <a href="/kills/t1">T1</a>
                                <a href="/kills/t2">T2</a>
                                <a href="/kills/t3">T3</a>
                                <div class="dropdown-section-header">Value</div>
                                <a href="/kills/5b">5B+</a>
                                <a href="/kills/10b">10B+</a>
                            </div>
                        </div>
                    </td>
                    {{#if followedEntities.hasEntities}}
                    <td class="nav-dropdown">
                        <div class="nav-dropdown-wrapper">
                            <a href="/entities">Entities</a>
                            <div class="nav-dropdown-content">
                                {{#if followedEntities.characterDetails.length}}
                                <div class="dropdown-section-header">Characters</div>
                                {{#each followedEntities.characterDetails}}
                                <a href="/character/{{this.id}}">{{this.name}}</a>
                                {{/each}}
                                {{/if}}
                                {{#if followedEntities.corporationDetails.length}}
                                <div class="dropdown-section-header">Corporations</div>
                                {{#each followedEntities.corporationDetails}}
                                <a href="/corporation/{{this.id}}">{{this.name}} [{{this.ticker}}]</a>
                                {{/each}}
                                {{/if}}
                                {{#if followedEntities.allianceDetails.length}}
                                <div class="dropdown-section-header">Alliances</div>
                                {{#each followedEntities.allianceDetails}}
                                <a href="/alliance/{{this.id}}">{{this.name}} &lt;{{this.ticker}}&gt;</a>
                                {{/each}}
                                {{/if}}
                            </div>
                        </div>
                    </td>
                    {{/if}}
                    <td><a href="/wars">Wars</a></td>
                    <td><a href="/ai">AI</a></td>
                    <td><a href="/post">Post</a></td>
                    <td class="nav-search">
                        <div class="search-container">
                            <input
                                type="text"
                                id="globalSearch"
                                placeholder="Search... (Cmd/Ctrl+K)"
                                autocomplete="off"
                            />
                            <div id="searchResults" class="search-results"></div>
                        </div>
                    </td>
                    <td class="nav-dropdown nav-help">
                        <div class="nav-dropdown-wrapper">
                            <a href="#">?</a>
                            <div class="nav-dropdown-content">
                                <a href="/docs">Docs</a>
                                <a href="/about">About</a>
                                <a href="/status">Status</a>
                            </div>
                        </div>
                    </td>
                    <td class="nav-user">
                        <div class="auth-block auth-logged-in" data-auth-section="in" style="display:none;">
                            <div class="avatar-trigger">
                                <img data-auth-avatar src="{{config.imageServerUrl}}/characters/0/portrait?size=64" alt="User portrait" loading="lazy" />
                            </div>
                            <div class="user-dropdown">
                                <div class="user-header">
                                    <img data-auth-avatar-large src="{{config.imageServerUrl}}/characters/0/portrait?size=128" alt="User portrait" loading="lazy" />
                                    <div class="user-meta">
                                        <div class="name" data-auth-name>...</div>
                                        <div class="line" data-auth-corp style="display:none;"></div>
                                        <div class="line" data-auth-alliance style="display:none;"></div>
                                    </div>
                                </div>
                                <div class="user-actions">
                                    <a data-auth-profile href="#">Profile</a>
                                    <a href="/settings">Settings</a>
                                    <a data-auth-admin href="/admin" style="display:none;">Admin</a>
                                    <a data-auth-logout href="/logout">Logout</a>
                                </div>
                            </div>
                        </div>

                        <div class="auth-block auth-logged-out" data-auth-section="out">
                            <div class="avatar-trigger">
                                <img src="{{config.imageServerUrl}}/characters/0/portrait?size=64" alt="Login placeholder portrait" loading="lazy" />
                            </div>
                            <div class="user-dropdown">
                                <div class="user-header">
                                    <img src="{{config.imageServerUrl}}/characters/0/portrait?size=128" alt="Login placeholder portrait" loading="lazy" />
                                    <div class="user-meta">
                                        <div class="name">Login</div>
                                        <div class="line">Choose how you want to log in</div>
                                    </div>
                                </div>
                                <div class="user-actions">
                                    <a data-auth-login href="/login?killmailAccess=1">With killmail access</a>
                                    <a data-auth-login href="/login?killmailAccess=0">Without killmail access</a>
                                </div>
                            </div>
                        </div>
                    </td>
                </tr>
            </table>
        </nav>

        <!-- Main Content Area -->
        <main>
            {{{body}}}
        </main>

        <!-- Footer -->
        <footer>
            <p>
                Powered by <strong>EDK</strong>
                {{#if config.copyright}}{{config.copyright}}{{else}}&copy; 2025{{/if}}
            </p>
            {{#if config.showVersion}}
            <p style="color: var(--color-text-tertiary); margin: 5px 0 0 0; font-size: 9px;">
                Version {{version}} | Build {{buildDate}}
            </p>
            {{/if}}
            {{#if performance}}
            <p style="color: #555; margin: 5px 0 0 0; font-size: 9px;">
                Page loaded in {{performance.totalTime}}ms
                {{#if performance.queryCount}}
                | {{performance.queryCount}} {{#ifEquals performance.queryCount 1}}query{{else}}queries{{/ifEquals}} ({{performance.dbTime}}ms avg)
                {{/if}}
                {{#if performance.templateTime}}
                | Template: {{performance.templateTime}}ms
                {{/if}}
                {{#if performance.layoutRenderTime}}
                | Layout: {{performance.layoutRenderTime}}ms
                {{/if}}
            </p>
            {{/if}}
        </footer>
    </div>

    {{#if debug}}
    {{> partials/debug-bar}}
    {{/if}}

    <!-- Scripts -->
    <script type="text/javascript">
        // Toggle popup display
        function ReverseContentDisplay(id) {
            var element = document.getElementById(id);
            if (element.style.display === 'none' || element.style.display === '') {
                element.style.display = 'block';
            } else {
                element.style.display = 'none';
            }
        }

        // Clickable table rows
        document.addEventListener('DOMContentLoaded', function() {
            var clickableRows = document.querySelectorAll('tr[onclick]');
            clickableRows.forEach(function(row) {
                row.style.cursor = 'pointer';
            });
        });
    </script>

    {{#if additionalJS}}
    {{#each additionalJS}}
    <script type="text/javascript" src="{{this}}"></script>
    {{/each}}
    {{/if}}

    <script>
      window.__EDK_IMAGE_URL = '{{config.imageServerUrl}}';
      (function setWsUrl() {
        const wsFromEnv = '{{env.WS_URL}}';
        if (wsFromEnv && wsFromEnv !== 'undefined' && wsFromEnv !== 'null') {
          const trimmed = wsFromEnv.endsWith('/ws') ? wsFromEnv : `${wsFromEnv}/ws`;
          window.__EDK_WS_URL = trimmed;
        }
      })();
    </script>
    <script type="text/javascript" src="/ws-ident.js"></script>
    <script type="text/javascript" src="/site-ws.js"></script>
    <script type="text/javascript" src="/auth-nav.js"></script>
    <script type="text/javascript" src="/auth-state.js"></script>
    <script type="text/javascript" src="/auth-ws.js"></script>

    {{#if debug}}
    <!-- Dev Hot Reload -->
    <script type="text/javascript" src="/dev-reload.js"></script>
    {{/if}}

    <!-- Fitting Wheel Script -->
    <script type="text/javascript" src="/fitting-wheel.js"></script>

    <!-- Items Sorting Script -->
    <script type="text/javascript" src="/items-sorting.js"></script>

    <!-- Search Script -->
    <script type="text/javascript" src="/search-history.js"></script>
    <script type="text/javascript" src="/search.js"></script>
    <script type="text/javascript" src="/spotlight-search.js"></script>

    <!-- Dropdown Positioning Script -->
    <script type="text/javascript">
        document.addEventListener('DOMContentLoaded', function() {
            // Dynamically position dropdowns based on screen position
            const dropdowns = document.querySelectorAll('.nav-dropdown');

            dropdowns.forEach(function(dropdown) {
                const content = dropdown.querySelector('.nav-dropdown-content');
                if (!content) return;

                dropdown.addEventListener('mouseenter', function() {
                    const rect = dropdown.getBoundingClientRect();
                    const screenWidth = window.innerWidth;
                    const midpoint = screenWidth / 2;

                    // If dropdown is in right half of screen, align to right
                    if (rect.left > midpoint) {
                        content.classList.add('align-right');
                    } else {
                        content.classList.remove('align-right');
                    }
                });
            });
        });
    </script>

    <!-- Expand/Collapse Component Library -->
    <script src="/expand-collapse.js"></script>

    {{{footerContent}}}
</body>
</html>
